import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class SpeedBoost extends Actor
{
    // Constructor for speed boost
    public SpeedBoost()
    {
        getImage().scale(getImage().getWidth()/3, getImage().getHeight()/3);
    }
}
